# Size Tracker - MVP 1

Android-приложение для отслеживания веса и калорий.

## Функционал

### Реализованные возможности ✅

- **Онбординг**: Ввод начальных данных пользователя (вес, рост, возраст, пол)
- **Автоматический расчет калорий**: По формуле Миффлина-Сан Жеора
- **Отслеживание веса**: Запись и просмотр истории изменений веса
- **Отслеживание калорий**: Учет потребленных калорий за день
- **Главный экран**: Отображение текущего прогресса
- **История**: Просмотр всех записей веса и калорий

## Технологии

- **Язык**: Kotlin
- **UI**: Jetpack Compose + Material 3
- **База данных**: Room Database
- **Архитектура**: MVVM
- **Навигация**: Jetpack Navigation Compose
- **Минимальная версия Android**: 7.0 (API 24)

## Структура проекта

```
src/main/kotlin/com/example/sizetracker/
├── data/
│   ├── dao/                    # Data Access Objects
│   ├── database/               # Room Database
│   ├── entity/                 # Сущности БД
│   └── repository/             # Репозитории
├── ui/
│   ├── navigation/             # Навигация
│   ├── screens/                # Экраны приложения
│   ├── theme/                  # Тема приложения
│   └── viewmodel/              # ViewModels
├── utils/                      # Утилиты (расчет калорий)
└── MainActivity.kt             # Главная Activity
```

## База данных

### Таблицы

1. **user_profile**: Профиль пользователя
2. **weight_entries**: Записи веса
3. **calorie_entries**: Записи калорий

## Формула расчета калорий

Используется формула Миффлина-Сан Жеора:

**Для мужчин:**
```
BMR = 10 × вес(кг) + 6.25 × рост(см) - 5 × возраст(г) + 5
```

**Для женщин:**
```
BMR = 10 × вес(кг) + 6.25 × рост(см) - 5 × возраст(г) - 161
```

Итоговая норма = BMR × коэффициент активности (по умолчанию 1.2)

## Локальный запуск в IntelliJ IDEA

### Требования

- **IntelliJ IDEA** 2023.1+ или **Android Studio** Hedgehog+
- **JDK 17** (рекомендуется Temurin или Oracle JDK)
- **Android SDK** с API Level 24+
- **Git**

### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/AlexandrKudrya/Size.git
cd Size
```

### Шаг 2: Открытие проекта в IDEA

1. Откройте IntelliJ IDEA
2. Выберите **File → Open**
3. Найдите и выберите папку проекта `Size`
4. Нажмите **OK**

IDEA автоматически:
- Определит проект как Android Gradle проект
- Скачает необходимые зависимости
- Настроит Android SDK (если настроен)

### Шаг 3: Настройка Android SDK

Если Android SDK не настроен:

1. Перейдите в **File → Project Structure → Project Settings → Project**
2. Установите **SDK**: JDK 17
3. Перейдите в **File → Project Structure → Platform Settings → SDKs**
4. Нажмите **+** → **Android SDK**
5. Выберите путь к Android SDK (обычно `~/Android/Sdk`)

### Шаг 4: Синхронизация Gradle

1. Откройте файл `build.gradle.kts`
2. IDEA предложит синхронизировать проект - нажмите **Sync Now**
3. Дождитесь завершения загрузки зависимостей

### Шаг 5: Запуск приложения

#### Вариант А: На эмуляторе

1. Создайте Android Virtual Device (AVD):
   - **Tools → Device Manager → Create Device**
   - Выберите устройство (например, Pixel 6)
   - Выберите System Image (API 24+, рекомендуется API 34)
2. Выберите созданный эмулятор в верхней панели
3. Нажмите **Run** (зеленая стрелка) или `Shift + F10`

#### Вариант Б: На физическом устройстве

1. Включите **Режим разработчика** на Android устройстве:
   - Настройки → О телефоне → Нажмите 7 раз на "Номер сборки"
2. Включите **Отладку по USB**:
   - Настройки → Для разработчиков → Отладка по USB
3. Подключите устройство к компьютеру через USB
4. Выберите устройство в верхней панели IDEA
5. Нажмите **Run**

### Шаг 6: Сборка APK вручную

#### Debug APK (для тестирования)

```bash
./gradlew assembleDebug
```

APK будет в: `build/outputs/apk/debug/`

#### Release APK (для релиза)

```bash
./gradlew assembleRelease
```

APK будет в: `build/outputs/apk/release/`

### Полезные команды Gradle

```bash
# Очистка build директории
./gradlew clean

# Запуск lint проверки
./gradlew lintDebug

# Запуск unit тестов
./gradlew testDebugUnitTest

# Установка на подключенное устройство
./gradlew installDebug

# Полная сборка с тестами
./gradlew build
```

## CI/CD с GitHub Actions

Проект настроен с автоматическими GitHub Actions workflows:

### 🔄 Android CI (`.github/workflows/android-build.yml`)

**Триггеры:**
- Push в ветки `main`, `master`, `develop`, `claude/**`
- Pull requests в `main`, `master`, `develop`

**Действия:**
- ✅ Lint проверка кода
- ✅ Запуск unit тестов
- ✅ Сборка Debug APK
- ✅ Сборка Release APK
- ✅ Загрузка артефактов (APK, отчеты)

**Артефакты:**
- `app-debug` - Debug APK для тестирования
- `app-release` - Release APK (unsigned)
- `lint-report` - Отчет по lint
- `test-report` - Отчет по тестам

### 🚀 Android Release (`.github/workflows/android-release.yml`)

**Триггеры:**
- Push тега вида `v*` (например, `v1.0.0`)

**Действия:**
- ✅ Сборка Release APK
- ✅ Создание GitHub Release
- ✅ Автоматическая публикация APK

**Создание релиза:**

```bash
git tag v1.0.0
git push origin v1.0.0
```

### 📊 Code Quality (`.github/workflows/code-quality.yml`)

**Триггеры:**
- Pull requests в `main`, `master`, `develop`

**Действия:**
- ✅ Detekt статический анализ
- ✅ Lint проверка
- ✅ Комментарии в PR с результатами

### Скачивание APK из Actions

1. Перейдите в **Actions** на GitHub
2. Выберите успешный workflow run
3. Прокрутите вниз до секции **Artifacts**
4. Скачайте `app-debug.zip` или `app-release.zip`
5. Распакуйте и установите APK на устройство

## Troubleshooting

### Проблема: "SDK location not found"

**Решение:**
1. Создайте файл `local.properties` в корне проекта
2. Добавьте путь к Android SDK:
   ```properties
   sdk.dir=/Users/YOUR_USERNAME/Library/Android/sdk  # macOS
   sdk.dir=C\:\\Users\\YOUR_USERNAME\\AppData\\Local\\Android\\Sdk  # Windows
   sdk.dir=/home/YOUR_USERNAME/Android/Sdk  # Linux
   ```

### Проблема: "Unable to locate Android SDK"

**Решение:**
1. Установите Android SDK через Android Studio:
   - Скачайте [Android Studio](https://developer.android.com/studio)
   - Запустите и пройдите установку SDK
2. Или установите через `sdkmanager`:
   ```bash
   sdkmanager "platforms;android-34" "build-tools;34.0.0"
   ```

### Проблема: "Gradle sync failed"

**Решение:**
1. Проверьте подключение к интернету
2. Очистите кеш Gradle:
   ```bash
   ./gradlew clean --refresh-dependencies
   ```
3. Удалите папки `.gradle` и `build`:
   ```bash
   rm -rf .gradle build
   ```

### Проблема: Приложение крашится при запуске

**Решение:**
1. Проверьте логи в **Logcat** (View → Tool Windows → Logcat)
2. Убедитесь, что версия Android на устройстве >= 7.0 (API 24)
3. Пересоберите проект:
   ```bash
   ./gradlew clean assembleDebug
   ```

### Проблема: Ошибка "Compose compiler version mismatch"

**Решение:**
Убедитесь, что версии Kotlin и Compose совместимы. В `build.gradle.kts` должны быть:
- Kotlin: 1.9.20
- Compose Compiler: 1.5.4

## Критерии готовности MVP 1

- ✅ Можно ввести вес и сохранить
- ✅ Можно ввести калории и сохранить
- ✅ Видно итог калорий за день
- ✅ Видно последний вес и прогресс к цели
- ✅ Данные сохраняются локально
- ✅ Можно посмотреть историю

## Экраны

1. **Онбординг**: Первый запуск, ввод данных пользователя
2. **Главный экран**: Текущий вес, калории за день, кнопки действий
3. **Добавление веса**: Ввод нового значения веса
4. **Добавление калорий**: Ввод потребленных калорий
5. **История**: Просмотр всех записей с возможностью удаления

## Автор

Создано с помощью Claude Code
